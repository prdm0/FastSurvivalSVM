% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bagging.R, R/bagging_backup.R
\name{predict.fastsvm_bagging}
\alias{predict.fastsvm_bagging}
\title{Predict from a fastsvm_bagging ensemble}
\usage{
\method{predict}{fastsvm_bagging}(object, newdata, ...)

\method{predict}{fastsvm_bagging}(object, newdata, ...)
}
\arguments{
\item{object}{An object of class \code{"fastsvm_bagging"} returned by
\code{fastsvm_bagging_fit()}.}

\item{newdata}{A \code{data.frame} containing the covariates used in
training (must include all columns listed in \code{object$x_cols}).}

\item{...}{Additional arguments (unused; included for S3 compatibility).}
}
\value{
A numeric vector of ensemble predictions, one value per row of
  \code{newdata}.

A numeric vector of ensemble predictions, one value per row of
  \code{newdata}.
}
\description{
Compute ensemble predictions for a \code{"fastsvm_bagging"} object as the
average of the predictions of its base learners.

Compute ensemble predictions for a \code{"fastsvm_bagging"} object as the
average of the predictions of its base learners.
}
\details{
This is compatible with both ranking (\code{rank_ratio = 1}) and regression
(\code{rank_ratio < 1}) modes of \code{FastKernelSurvivalSVM}. For pure
regression examples, see \code{\link{fastsvm_bagging_fit}}.

This is compatible with both ranking (\code{rank_ratio = 1}) and regression
(\code{rank_ratio < 1}) modes of \code{FastKernelSurvivalSVM}. For pure
regression examples, see \code{\link{fastsvm_bagging_fit}}.
}
\examples{
if (reticulate::py_module_available("sksurv")) {
  set.seed(123)
  df <- data_generation(n = 200L, prop_cen = 0.2)

  bag <- fastsvm_bagging_fit(
    data         = df,
    time_col     = "tempo",
    delta_col    = "cens",
    n_estimators = 8,
    mtry         = 2,
    kernels      = "rbf",
    alpha        = 1,
    rank_ratio   = 0
  )

  preds <- predict(bag, df)
  head(preds)
}

if (reticulate::py_module_available("sksurv")) {
  set.seed(123)
  df <- data_generation(n = 200L, prop_cen = 0.2)

  bag <- fastsvm_bagging_fit(
    data         = df,
    time_col     = "tempo",
    delta_col    = "cens",
    n_estimators = 8,
    mtry         = 2,
    kernels      = "rbf",
    alpha        = 1,
    rank_ratio   = 0
  )

  preds <- predict(bag, df)
  head(preds)
}

}

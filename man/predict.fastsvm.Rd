% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_kernel_surv_svm.R
\name{predict.fastsvm}
\alias{predict.fastsvm}
\title{Predict risk scores or transformed survival times from a fastsvm model}
\usage{
\method{predict}{fastsvm}(object, newdata, ...)
}
\arguments{
\item{object}{An object of class \code{"fastsvm"} returned by
\code{fast_kernel_surv_svm_fit()}.}

\item{newdata}{A \code{data.frame} containing the covariate columns used
in the original fit (same names as in \code{object$x_cols}).}

\item{...}{Not used (for S3 compatibility).}
}
\value{
A numeric vector of predictions:
  \itemize{
    \item If \code{rank_ratio = 1}, higher values indicate higher risk.
    \item If \code{rank_ratio < 1}, values correspond to transformed
          survival times (lower = shorter survival, higher = longer).
  }
}
\description{
Predict risk scores or transformed survival times from a fastsvm model
}
\examples{
if (reticulate::py_module_available("sksurv")) {
  set.seed(1)
  n <- 50
  df <- data.frame(
    time   = rexp(n, 0.1),
    status = rbinom(n, 1, 0.6),
    x1     = rnorm(n),
    x2     = rnorm(n)
  )

  fit <- fast_kernel_surv_svm_fit(
    data      = df,
    time_col  = "time",
    delta_col = "status",
    kernel    = "rbf"
  )

  preds <- predict(fit, df)
  head(preds)
}

}

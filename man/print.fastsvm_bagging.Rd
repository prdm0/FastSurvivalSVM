% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bagging.R, R/bagging_backup.R
\name{print.fastsvm_bagging}
\alias{print.fastsvm_bagging}
\title{Print method for fastsvm_bagging objects}
\usage{
\method{print}{fastsvm_bagging}(x, ...)

\method{print}{fastsvm_bagging}(x, ...)
}
\arguments{
\item{x}{An object of class \code{"fastsvm_bagging"}.}

\item{...}{Not used.}
}
\description{
Display a compact summary of a \code{"fastsvm_bagging"} ensemble, including
the number of base learners, number of covariates, and key configuration
parameters.

Display a compact summary of a \code{"fastsvm_bagging"} ensemble, including
the number of base learners, number of covariates, and key configuration
parameters.
}
\examples{
if (reticulate::py_module_available("sksurv")) {
  set.seed(123)
  df <- data_generation(n = 100L, prop_cen = 0.2)

  bag <- fastsvm_bagging_fit(
    data         = df,
    time_col     = "tempo",
    delta_col    = "cens",
    n_estimators = 5,
    mtry         = 2,
    kernels      = "rbf",
    alpha        = 1,
    rank_ratio   = 0
  )

  print(bag)
}

if (reticulate::py_module_available("sksurv")) {
  set.seed(123)
  df <- data_generation(n = 100L, prop_cen = 0.2)

  bag <- fastsvm_bagging_fit(
    data         = df,
    time_col     = "tempo",
    delta_col    = "cens",
    n_estimators = 5,
    mtry         = 2,
    kernels      = "rbf",
    alpha        = 1,
    rank_ratio   = 0
  )

  print(bag)
}

}
